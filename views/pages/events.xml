<odoo>
  <template id="events_page">
    <t t-call="website.layout">

      <style>
        /* ========== Variables ========== */
        :root {
          --events-primary: #1A4D2E;
          --events-primary-light: #2D7A4D;
          --events-bg: #f9fafb;
          --events-card-bg: #ffffff;
          --events-text: #111827;
          --events-text-muted: #6b7280;
          --events-border: #e5e7eb;
          --events-shadow: rgba(26, 77, 46, 0.08);
          --events-radius: 16px;
          --events-radius-sm: 8px;
          --events-transition: 0.3s ease;
        }

        #events-page {
          background-color: var(--events-bg);
        }

        #events-page a {
          text-decoration: none !important;
          color: inherit;
        }

        /* ========== Hero Section ========== */
        .events-hero {
          position: relative;
          padding: 80px 0 60px;
          background: linear-gradient(135deg, var(--events-primary) 0%, var(--events-primary-light) 100%);
          overflow: hidden;
        }

        .events-hero::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          right: 0;
          bottom: 0;
          background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
          opacity: 0.5;
        }

        .events-hero-content {
          position: relative;
          z-index: 1;
          max-width: 1280px;
          margin: 0 auto;
          padding: 0 24px;
          text-align: center;
        }

        .events-hero h1 {
          font-size: 3rem;
          font-weight: 800;
          color: white;
          margin-bottom: 16px;
          letter-spacing: -0.02em;
        }

        .events-hero p {
          font-size: 1.125rem;
          color: rgba(255, 255, 255, 0.85);
          max-width: 600px;
          margin: 0 auto;
        }

        /* ========== Filter Bar ========== */
        .filter-bar-container {
          max-width: 1280px;
          margin: -28px auto 0;
          padding: 0 24px;
          position: relative;
          z-index: 10;
        }

        .filter-bar {
          display: flex;
          align-items: center;
          gap: 12px;
          background: var(--events-card-bg);
          padding: 16px 20px;
          border-radius: var(--events-radius);
          box-shadow: 0 4px 24px var(--events-shadow), 0 1px 3px rgba(0, 0, 0, 0.05);
          flex-wrap: wrap;
        }

        .filter-bar-label {
          font-size: 0.875rem;
          font-weight: 600;
          color: var(--events-text-muted);
          margin-right: 8px;
        }

        /* ========== Filter Dropdown ========== */
        .filter-dropdown {
          position: relative;
        }

        .filter-dropdown-trigger {
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 10px 16px;
          background: #f3f4f6;
          border: 1px solid var(--events-border);
          border-radius: var(--events-radius-sm);
          font-size: 0.875rem;
          font-weight: 500;
          color: var(--events-text);
          cursor: pointer;
          transition: all var(--events-transition);
          min-width: 140px;
          justify-content: space-between;
        }

        .filter-dropdown-trigger:hover {
          background: #e5e7eb;
          border-color: #d1d5db;
        }

        .filter-dropdown-trigger.active {
          background: var(--events-primary);
          color: white;
          border-color: var(--events-primary);
        }

        .filter-dropdown-trigger .chevron {
          transition: transform var(--events-transition);
        }

        .filter-dropdown.open .filter-dropdown-trigger .chevron {
          transform: rotate(180deg);
        }

        .filter-dropdown-menu {
          position: absolute;
          top: calc(100% + 8px);
          left: 0;
          min-width: 180px;
          background: var(--events-card-bg);
          border-radius: var(--events-radius-sm);
          box-shadow: 0 10px 40px rgba(0, 0, 0, 0.12);
          border: 1px solid var(--events-border);
          z-index: 100;
          opacity: 0;
          visibility: hidden;
          transform: translateY(-10px);
          transition: all 0.2s ease;
        }

        .filter-dropdown.open .filter-dropdown-menu {
          opacity: 1;
          visibility: visible;
          transform: translateY(0);
        }

        .filter-dropdown-item {
          display: flex;
          align-items: center;
          gap: 10px;
          padding: 12px 16px;
          font-size: 0.875rem;
          color: var(--events-text);
          cursor: pointer;
          transition: background var(--events-transition);
        }

        .filter-dropdown-item:first-child {
          border-radius: var(--events-radius-sm) var(--events-radius-sm) 0 0;
        }

        .filter-dropdown-item:last-child {
          border-radius: 0 0 var(--events-radius-sm) var(--events-radius-sm);
        }

        .filter-dropdown-item:hover {
          background: #f3f4f6;
        }

        .filter-dropdown-item.selected {
          background: rgba(26, 77, 46, 0.08);
          color: var(--events-primary);
          font-weight: 600;
        }

        .filter-dropdown-item .check-icon {
          opacity: 0;
          color: var(--events-primary);
        }

        .filter-dropdown-item.selected .check-icon {
          opacity: 1;
        }

        /* ========== Search Input ========== */
        .search-input-container {
          display: flex;
          align-items: center;
          flex: 1;
          min-width: 200px;
          max-width: 320px;
          margin-left: auto;
        }

        .search-input-wrapper {
          position: relative;
          width: 100%;
        }

        .search-input-wrapper .search-icon {
          position: absolute;
          left: 14px;
          top: 50%;
          transform: translateY(-50%);
          color: var(--events-text-muted);
          pointer-events: none;
        }

        .search-input {
          width: 100%;
          padding: 10px 14px 10px 42px;
          background: #f3f4f6;
          border: 1px solid var(--events-border);
          border-radius: var(--events-radius-sm);
          font-size: 0.875rem;
          color: var(--events-text);
          transition: all var(--events-transition);
        }

        .search-input:focus {
          outline: none;
          border-color: var(--events-primary);
          box-shadow: 0 0 0 3px rgba(26, 77, 46, 0.1);
          background: white;
        }

        .search-input::placeholder {
          color: var(--events-text-muted);
        }

        /* ========== Active Filters Chips ========== */
        .active-filters {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          padding: 12px 0 0;
        }

        .active-filters-label {
          font-size: 0.8rem;
          color: var(--events-text-muted);
        }

        .filter-chip {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 6px 12px;
          background: rgba(26, 77, 46, 0.1);
          border-radius: 50px;
          font-size: 0.8rem;
          font-weight: 500;
          color: var(--events-primary);
        }

        .filter-chip button {
          display: flex;
          align-items: center;
          justify-content: center;
          background: none;
          border: none;
          padding: 0;
          cursor: pointer;
          color: var(--events-primary);
          opacity: 0.7;
          transition: opacity var(--events-transition);
        }

        .filter-chip button:hover {
          opacity: 1;
        }

        .clear-all-btn {
          padding: 6px 12px;
          background: none;
          border: 1px dashed var(--events-border);
          border-radius: 50px;
          font-size: 0.8rem;
          color: var(--events-text-muted);
          cursor: pointer;
          transition: all var(--events-transition);
        }

        .clear-all-btn:hover {
          border-color: #ef4444;
          color: #ef4444;
          background: rgba(239, 68, 68, 0.05);
        }

        /* ========== Events Grid ========== */
        .events-content {
          max-width: 1280px;
          margin: 0 auto;
          padding: 32px 24px 80px;
        }

        .events-grid {
          display: grid;
          grid-template-columns: repeat(4, 1fr);
          gap: 24px;
        }

        @media (max-width: 1200px) {
          .events-grid {
            grid-template-columns: repeat(3, 1fr);
          }
        }

        @media (max-width: 900px) {
          .events-grid {
            grid-template-columns: repeat(2, 1fr);
          }
        }

        @media (max-width: 600px) {
          .events-grid {
            grid-template-columns: 1fr;
          }
          
          .events-hero h1 {
            font-size: 2rem;
          }
          
          .filter-bar {
            flex-direction: column;
            align-items: stretch;
          }
          
          .search-input-container {
            max-width: none;
            margin-left: 0;
          }
        }

        /* ========== Section Headers ========== */
        .events-section {
          margin-bottom: 48px;
        }

        .section-header {
          margin-bottom: 24px;
        }

        .section-title-wrap {
          display: flex;
          align-items: center;
          gap: 12px;
          margin-bottom: 8px;
          flex-wrap: wrap;
        }

        .section-badge {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 6px 12px;
          border-radius: 50px;
          font-size: 0.7rem;
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 0.5px;
        }

        .section-badge.live {
          background: linear-gradient(135deg, #ef4444, #dc2626);
          color: white;
          animation: pulse-badge 2s infinite;
        }

        .section-badge.live i {
          font-size: 8px;
          animation: blink 1s infinite;
        }

        @keyframes blink {
          0%, 100% { opacity: 1; }
          50% { opacity: 0.3; }
        }

        @keyframes pulse-badge {
          0%, 100% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4); }
          50% { box-shadow: 0 0 0 8px rgba(239, 68, 68, 0); }
        }

        .section-badge.upcoming {
          background: linear-gradient(135deg, var(--events-primary), var(--events-primary-light));
          color: white;
        }

        .section-badge.finished {
          background: #6b7280;
          color: white;
        }

        .section-title {
          font-size: 1.75rem;
          font-weight: 800;
          color: var(--events-text);
          margin: 0;
          letter-spacing: -0.02em;
        }

        .section-subtitle {
          font-size: 0.95rem;
          color: var(--events-text-muted);
          margin: 0;
        }

        @media (max-width: 600px) {
          .section-title {
            font-size: 1.4rem;
          }
        }

        /* ========== Event Card ========== */
        .event-card {
          background: var(--events-card-bg);
          border-radius: var(--events-radius);
          overflow: hidden;
          box-shadow: 0 2px 8px var(--events-shadow);
          border: 1px solid var(--events-border);
          transition: all var(--events-transition);
          height: 420px;
          display: flex;
          flex-direction: column;
        }

        .event-card:hover {
          transform: translateY(-6px);
          box-shadow: 0 12px 32px rgba(26, 77, 46, 0.12);
        }

        .event-card.d-none {
          display: none !important;
        }

        .event-card-image {
          position: relative;
          height: 200px;
          overflow: hidden;
        }

        .event-card-image img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          transition: transform 0.5s ease;
        }

        .event-card:hover .event-card-image img {
          transform: scale(1.08);
        }

        .event-card-badges {
          position: absolute;
          top: 12px;
          left: 12px;
          right: 12px;
          display: flex;
          gap: 8px;
          flex-wrap: wrap;
        }

        .badge {
          padding: 5px 10px;
          border-radius: 6px;
          font-size: 0.7rem;
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 0.5px;
        }

        .badge-status {
          background: rgba(45, 122, 77, 0.95);
          color: white;
        }

        .badge-status.ongoing {
          background: rgba(234, 179, 8, 0.95);
          color: #713f12;
        }

        .badge-status.finished {
          background: rgba(107, 114, 128, 0.9);
          color: white;
        }

        .badge-format {
          background: rgba(255, 255, 255, 0.95);
          color: var(--events-text);
        }

        .badge-format.online {
          background: rgba(59, 130, 246, 0.95);
          color: white;
        }

        .badge-format.hybrid {
          background: rgba(168, 85, 247, 0.95);
          color: white;
        }

        .event-card-content {
          padding: 20px;
          flex: 1;
          display: flex;
          flex-direction: column;
        }

        .event-card-category {
          font-size: 0.75rem;
          font-weight: 600;
          text-transform: uppercase;
          letter-spacing: 0.5px;
          color: var(--events-primary);
          margin-bottom: 8px;
        }

        .event-card-title {
          font-size: 1.1rem;
          font-weight: 700;
          color: var(--events-text);
          margin-bottom: 12px;
          line-height: 1.4;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
        }

        .event-card-meta {
          margin-top: auto;
          display: flex;
          flex-direction: column;
          gap: 8px;
        }

        .event-card-meta-item {
          display: flex;
          align-items: center;
          gap: 8px;
          font-size: 0.85rem;
          color: var(--events-text-muted);
        }

        .event-card-meta-item i {
          color: var(--events-primary);
          width: 16px;
        }

        .event-card-footer {
          padding: 16px 20px;
          border-top: 1px solid var(--events-border);
          display: flex;
          justify-content: space-between;
          align-items: center;
        }

        .event-card-price {
          font-size: 1rem;
          font-weight: 700;
          color: var(--events-primary);
        }

        .event-card-price.free {
          color: #16a34a;
        }

        /* ========== Empty State ========== */
        .empty-state {
          text-align: center;
          padding: 80px 24px;
          background: var(--events-card-bg);
          border-radius: var(--events-radius);
          border: 2px dashed var(--events-border);
          display: none;
        }

        .empty-state.show {
          display: block;
        }

        .empty-state-icon {
          width: 80px;
          height: 80px;
          background: #f3f4f6;
          border-radius: 50%;
          display: flex;
          align-items: center;
          justify-content: center;
          margin: 0 auto 24px;
        }

        .empty-state-icon i {
          font-size: 2rem;
          color: var(--events-text-muted);
        }

        .empty-state h3 {
          font-size: 1.25rem;
          font-weight: 700;
          color: var(--events-text);
          margin-bottom: 8px;
        }

        .empty-state p {
          color: var(--events-text-muted);
          max-width: 400px;
          margin: 0 auto 24px;
        }

        .empty-state-btn {
          background: none;
          border: none;
          color: var(--events-primary);
          font-weight: 600;
          cursor: pointer;
          padding: 0;
          transition: opacity var(--events-transition);
        }

        .empty-state-btn:hover {
          opacity: 0.7;
        }

        /* ========== Pagination ========== */
        .pagination {
          display: flex;
          justify-content: center;
          align-items: center;
          gap: 8px;
          margin-top: 48px;
        }

        .pagination.d-none {
          display: none !important;
        }

        .pagination-btn {
          display: flex;
          align-items: center;
          justify-content: center;
          width: 40px;
          height: 40px;
          background: var(--events-card-bg);
          border: 1px solid var(--events-border);
          border-radius: var(--events-radius-sm);
          font-size: 0.9rem;
          font-weight: 500;
          color: var(--events-text);
          cursor: pointer;
          transition: all var(--events-transition);
        }

        .pagination-btn:hover:not(:disabled):not(.disabled) {
          background: #f3f4f6;
          border-color: var(--events-primary);
        }

        .pagination-btn:disabled,
        .pagination-btn.disabled {
          opacity: 0.4;
          cursor: not-allowed;
        }

        .pagination-btn.active {
          background: var(--events-primary);
          color: white;
          border-color: var(--events-primary);
        }
      </style>

      <div id="events-page">

        <!-- Hero Section -->
        <section class="events-hero">
          <div class="events-hero-content">
            <h1>Jadwal Kegiatan</h1>
            <p>Temukan berbagai aktivitas inspiratif untuk pengembangan diri dan bisnis Anda.</p>
          </div>
        </section>

        <!-- Filter Bar -->
        <div class="filter-bar-container" id="events-grid">
          <div class="filter-bar">
            <span class="filter-bar-label">Filter:</span>
            
            <!-- Category Dropdown -->
            <div class="filter-dropdown" id="category-dropdown">
              <button class="filter-dropdown-trigger" data-dropdown="category">
                <span class="dropdown-label">Semua Kategori</span>
                <i class="fa fa-chevron-down chevron"></i>
              </button>
              <div class="filter-dropdown-menu">
                <t t-foreach="categories" t-as="cat">
                  <div t-attf-class="filter-dropdown-item #{'selected' if cat.get('id') == 'all' else ''}"
                       t-att-data-id="cat.get('id')"
                       data-type="category">
                    <i class="fa fa-check check-icon"></i>
                    <span><t t-esc="cat.get('label')"/></span>
                  </div>
                </t>
              </div>
            </div>

            <!-- Status Dropdown -->
            <div class="filter-dropdown" id="status-dropdown">
              <button class="filter-dropdown-trigger" data-dropdown="status">
                <span class="dropdown-label">Semua Status</span>
                <i class="fa fa-chevron-down chevron"></i>
              </button>
              <div class="filter-dropdown-menu">
                <t t-foreach="statuses" t-as="status">
                  <div t-attf-class="filter-dropdown-item #{'selected' if status.get('id') == 'all' else ''}"
                       t-att-data-id="status.get('id')"
                       data-type="status">
                    <i class="fa fa-check check-icon"></i>
                    <span><t t-esc="status.get('label')"/></span>
                  </div>
                </t>
              </div>
            </div>

            <!-- Format Dropdown -->
            <div class="filter-dropdown" id="format-dropdown">
              <button class="filter-dropdown-trigger" data-dropdown="format">
                <span class="dropdown-label">Semua Format</span>
                <i class="fa fa-chevron-down chevron"></i>
              </button>
              <div class="filter-dropdown-menu">
                <t t-foreach="formats" t-as="fmt">
                  <div t-attf-class="filter-dropdown-item #{'selected' if fmt.get('id') == 'all' else ''}"
                       t-att-data-id="fmt.get('id')"
                       data-type="format">
                    <i class="fa fa-check check-icon"></i>
                    <span><t t-esc="fmt.get('label')"/></span>
                  </div>
                </t>
              </div>
            </div>

            <!-- Search -->
            <div class="search-input-container">
              <div class="search-input-wrapper">
                <i class="fa fa-search search-icon"></i>
                <input type="text" 
                       id="search-input"
                       placeholder="Cari kegiatan..." 
                       class="search-input"/>
              </div>
            </div>
          </div>

          <!-- Active Filter Chips -->
          <div class="active-filters" id="active-filters" style="display: none;">
            <span class="active-filters-label">Filter aktif:</span>
            <div id="filter-chips"></div>
            <button class="clear-all-btn" id="clear-all-btn">Hapus Semua</button>
          </div>
        </div>

        <!-- Events Content -->
        <div class="events-content">
          
          <!-- Section: Jangan Sampai Kelewatan (Ongoing) -->
          <t t-if="ongoing_events">
            <div class="events-section" id="section-ongoing">
              <div class="section-header">
                <div class="section-title-wrap">
                  <span class="section-badge live"><i class="fa fa-circle"></i> LIVE</span>
                  <h2 class="section-title">Jangan Sampai Kelewatan</h2>
                </div>
                <p class="section-subtitle">Acara yang sedang berlangsung saat ini</p>
              </div>
              <div class="events-grid">
                <t t-foreach="ongoing_events" t-as="event">
                  <a t-att-href="'/acara/%s' % event.get('id')" 
                     class="event-card"
                     t-att-data-status="event.get('status')"
                     t-att-data-category="event.get('category')"
                     t-att-data-format="event.get('format')"
                     t-att-data-title="event.get('title')">
                    
                    <div class="event-card-image">
                      <img t-att-src="event.get('image')" t-att-alt="event.get('title')"/>
                      <div class="event-card-badges">
                        <span t-attf-class="badge badge-status #{event.get('status')}">Live</span>
                        <span t-attf-class="badge badge-format #{event.get('format')}">
                          <t t-esc="event.get('format')"/>
                        </span>
                      </div>
                    </div>

                    <div class="event-card-content">
                      <span class="event-card-category"><t t-esc="event.get('category')"/></span>
                      <h3 class="event-card-title"><t t-esc="event.get('title')"/></h3>
                      
                      <div class="event-card-meta">
                        <div class="event-card-meta-item">
                          <i class="fa fa-calendar"></i>
                          <span><t t-esc="event.get('date')"/></span>
                        </div>
                        <div class="event-card-meta-item">
                          <i class="fa fa-map-marker"></i>
                          <span><t t-esc="event.get('location')"/></span>
                        </div>
                      </div>
                    </div>

                    <div class="event-card-footer">
                      <span t-attf-class="event-card-price #{'free' if event.get('price') == 'Free' else ''}">
                        <t t-esc="event.get('price')"/>
                      </span>
                      <i class="fa fa-ticket" style="color: var(--events-primary);"></i>
                    </div>
                  </a>
                </t>
              </div>
            </div>
          </t>

          <!-- Section: Segera Hadir (Upcoming) -->
          <t t-if="upcoming_events">
            <div class="events-section" id="section-upcoming">
              <div class="section-header">
                <div class="section-title-wrap">
                  <span class="section-badge upcoming"><i class="fa fa-clock-o"></i> COMING SOON</span>
                  <h2 class="section-title">Segera Hadir</h2>
                </div>
                <p class="section-subtitle">Acara menarik yang akan datang</p>
              </div>
              <div class="events-grid">
                <t t-foreach="upcoming_events" t-as="event">
                  <a t-att-href="'/acara/%s' % event.get('id')" 
                     class="event-card"
                     t-att-data-status="event.get('status')"
                     t-att-data-category="event.get('category')"
                     t-att-data-format="event.get('format')"
                     t-att-data-title="event.get('title')">
                    
                    <div class="event-card-image">
                      <img t-att-src="event.get('image')" t-att-alt="event.get('title')"/>
                      <div class="event-card-badges">
                        <span t-attf-class="badge badge-status #{event.get('status')}">Soon</span>
                        <span t-attf-class="badge badge-format #{event.get('format')}">
                          <t t-esc="event.get('format')"/>
                        </span>
                      </div>
                    </div>

                    <div class="event-card-content">
                      <span class="event-card-category"><t t-esc="event.get('category')"/></span>
                      <h3 class="event-card-title"><t t-esc="event.get('title')"/></h3>
                      
                      <div class="event-card-meta">
                        <div class="event-card-meta-item">
                          <i class="fa fa-calendar"></i>
                          <span><t t-esc="event.get('date')"/></span>
                        </div>
                        <div class="event-card-meta-item">
                          <i class="fa fa-map-marker"></i>
                          <span><t t-esc="event.get('location')"/></span>
                        </div>
                      </div>
                    </div>

                    <div class="event-card-footer">
                      <span t-attf-class="event-card-price #{'free' if event.get('price') == 'Free' else ''}">
                        <t t-esc="event.get('price')"/>
                      </span>
                      <i class="fa fa-ticket" style="color: var(--events-primary);"></i>
                    </div>
                  </a>
                </t>
              </div>
            </div>
          </t>

          <!-- Section: Arsip (Finished) -->
          <t t-if="finished_events">
            <div class="events-section" id="section-finished">
              <div class="section-header">
                <div class="section-title-wrap">
                  <span class="section-badge finished"><i class="fa fa-check-circle"></i> ARSIP</span>
                  <h2 class="section-title">Kegiatan Selesai</h2>
                </div>
                <p class="section-subtitle">Dokumentasi acara yang telah berlangsung</p>
              </div>
              <div class="events-grid">
                <t t-foreach="finished_events" t-as="event">
                  <a t-att-href="'/acara/%s' % event.get('id')" 
                     class="event-card"
                     t-att-data-status="event.get('status')"
                     t-att-data-category="event.get('category')"
                     t-att-data-format="event.get('format')"
                     t-att-data-title="event.get('title')">
                    
                    <div class="event-card-image">
                      <img t-att-src="event.get('image')" t-att-alt="event.get('title')"/>
                      <div class="event-card-badges">
                        <span t-attf-class="badge badge-status #{event.get('status')}">Done</span>
                        <span t-attf-class="badge badge-format #{event.get('format')}">
                          <t t-esc="event.get('format')"/>
                        </span>
                      </div>
                    </div>

                    <div class="event-card-content">
                      <span class="event-card-category"><t t-esc="event.get('category')"/></span>
                      <h3 class="event-card-title"><t t-esc="event.get('title')"/></h3>
                      
                      <div class="event-card-meta">
                        <div class="event-card-meta-item">
                          <i class="fa fa-calendar"></i>
                          <span><t t-esc="event.get('date')"/></span>
                        </div>
                        <div class="event-card-meta-item">
                          <i class="fa fa-map-marker"></i>
                          <span><t t-esc="event.get('location')"/></span>
                        </div>
                      </div>
                    </div>

                    <div class="event-card-footer">
                      <span t-attf-class="event-card-price #{'free' if event.get('price') == 'Free' else ''}">
                        <t t-esc="event.get('price')"/>
                      </span>
                      <i class="fa fa-ticket" style="color: var(--events-primary);"></i>
                    </div>
                  </a>
                </t>
              </div>
            </div>
          </t>

          <!-- Empty State -->
          <div class="empty-state" id="empty-state">
            <div class="empty-state-icon">
              <i class="fa fa-search"></i>
            </div>
            <h3>Belum ada kegiatan ditemukan</h3>
            <p>Sepertinya belum ada kegiatan untuk filter yang Anda pilih. Coba sesuaikan filter atau cek lagi nanti ya!</p>
            <button class="empty-state-btn" id="reset-filter-btn">Tampilkan Semua Kegiatan</button>
          </div>

          <!-- Pagination -->
          <div class="pagination" id="pagination-controls"></div>

        </div>
      </div>
    </t>
  </template>
</odoo>
